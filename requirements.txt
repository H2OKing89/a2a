orjson
sh
typer
rich
pathvalidate
pydantic
pydantic-settings
PyYAML

# Audible API client (audible-cli backend)
audible
toml

# Audible scraper dependencies
httpx[http2]
beautifulsoup4
lxml
tenacity

# Text matching for title verification
rapidfuzz

# Environment variable loading
python-dotenv

# Jinja2 for report templates
Jinja2

# Testing
pytest>=9.0.0,<10.0.0
pytest-cov>=7.0.0,<8.0.0
pytest-asyncio>=1.3.0,<2.0.0

# Note: mediainfo is a system dependency, install with your OS package manager (e.g., sudo apt install mediainfo)
